<%= form_for @management, url: {action: :create} do |f| %>
  <%= f.label :subject, '科目' %> 
  <%= f.text_field :subject %> 
  <%= f.label :annual, '開講年次' %> 
  <%= f.password_field :annual %>
  <%= f.label :link, 'シラバスリンク' %> 
  <%= f.password_field :link %>
  <%= f.submit '登録' %>
<% end %>