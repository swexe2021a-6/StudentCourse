<% if flash[:notice] %>
  <%= flash[:notice] %>
<% end %>
<% @user.each do |e| %>
  <p>
    <%= e.student_num %>
    <%= e.pass %>
    <%= link_to '削除', user_path(e), method: :delete %>
    </p>
<% end %>
<nav>
  <div class = "nav_left"><%= link_to '新規ユーザ追加', new_user_path %></div>
  <div class = "nav_right"><% if current_user %>
    <%= link_to 'ログアウト', top_logout_path %>
  <% else %>
    <%= link_to 'ログイン', top_main_path %>
  <% end %></div>
</nav>

<div class = "main">
  <div class = content1>
    <% if current_user %>
      <strong><%= link_to '科目情報閲覧の方はこちら', valuation_indexView_path %></strong>
    <% else %>
      <strong><%= link_to '科目情報閲覧の方はこちら', top_main_path %></strong>
    <% end %>
  </div>
  <div class = content2>
    <% if current_user %>
      <strong><%= link_to '科目情報登録の方はこちら', valuation_index_path %></strong>
     <% else %>
      <strong><%= link_to '科目情報登録の方はこちら', top_main_path %></strong>
    <% end %>
  </div>
</div>


<style>
nav{
  width: 900px;
  text-align: center;
  background-color: #38b6ff;
}
.nav_left{
  float: left;
}
.nav_right{
  float: right;
}
.content1,.content2{
 width: 50%;
 height: 100px;
 
}
.content1 strong{
  background-color:#38b6ff;
  padding:20px 20px;
  border-radius: 15px;
}
.content2 strong{
  background-color:#38b6ff;
  padding:20px 20px;
  border-radius: 15px;
}
.main{
 display: flex;
 width:1100px;
 padding-top: 150px;
 text-align: center;
}
</style>